{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "build:ui": "rm -rf build && cd ../../test && npm run build --prod && cp -r build ../node/notes",
    "push:git": "git add . && git commit -m uibuild && git push origin main",
    "test": "NODE_ENV=test jest --verbose --runInBand"
  },
  "author": "Lucas Cordova",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "5.0.1",
    "colors": "1.4.0",
    "cors": "2.8.5",
    "dotenv": "8.2.0",
    "express": "4.17.1",
    "express-async-errors": "3.1.1",
    "mongoose": "5.11.17",
    "mongoose-unique-validator": "2.0.3"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "jest": "26.6.3",
    "nodemon": "2.0.7",
    "supertest": "6.1.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}

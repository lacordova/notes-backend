{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "rm -rf build && cd ../../test && npm run build --prod && cp -r build ../node/notes"
  },
  "author": "Lucas Cordova",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "cors": "2.8.5",
    "dotenv": "8.2.0",
    "express": "^4.17.1",
    "mongoose": "5.11.17",
    "mongoose-unique-validator": "2.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
